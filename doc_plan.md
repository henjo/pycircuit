Goal: Add extremely detailed, deep architectural and mathematical comments throughout the codebase.

Target Files:
1. `pycircuit/circuit/func.py`
2. `pycircuit/circuit/symbolic.py` and `pycircuit/circuit/_symbolictoolkit.py`

Guidelines for the subagent:
- For `func.py`, add deep comments explaining the mathematical functions generated by `Sin`, `Pulse`, `Exp`, `PWL`, etc., and how they map continuous time $t$ to values.
- For the symbolic toolkit, document how `Sympy` is used to build analytical expressions for Jacobians and charge equations without evaluating them numerically. Explain how nodes are mapped to Sympy symbols.
- Use `multi_replace_file_content` to inject these comments directly into the Python files.
